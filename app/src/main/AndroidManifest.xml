<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.City_Finder"
        tools:targetApi="31">

        <!-- This is the starting screen of the app -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- All other activities are declared here -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".LoadingActivity"
            android:exported="false"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar"/>

        <activity
            android:name=".ResultsActivity"
            android:exported="false" />

        <activity
            android:name=".ExploreActivity"
            android:exported="false"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar"/>

        <activity
            android:name=".DetailsActivity"
            android:exported="false" />

        <activity
            android:name=".WeatherActivity"
            android:exported="false" />


        <activity
            android:name=".CuisineLoadingActivity"
            android:exported="false"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar"/>

        <activity
            android:name=".CuisineActivity"
            android:exported="false" />

        <activity
            android:name=".RecipeDetailActivity"
            android:exported="false" />

        <!-- In AndroidManifest.xml -->
        <activity
            android:name=".CurrencySelectionActivity"
            android:exported="false" />
        <activity
            android:name=".CurrencyDetailsActivity"
            android:exported="false" />

        <activity
            android:name=".DemographicsActivity"
            android:exported="false"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".DadJokeActivity"
            android:exported="false"
            tools:ignore="LockedOrientationActivity" />

        <activity android:name=".SpotsActivity" android:exported="false" />
        <activity android:name=".SpotDetailActivity" android:exported="false" />
        <activity android:name=".PhrasesActivity" tools:ignore="LockedOrientationActivity" />

    </application>

</manifest>
